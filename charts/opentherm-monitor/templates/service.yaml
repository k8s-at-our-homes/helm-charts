apiVersion: v1
kind: Service
metadata:
  name: {{ template "common.fullname" . }}
  labels: {{ include "openthermMonitor.objectLabels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector: {{ include "openthermMonitor.selectorLabels" . | nindent 4 }}
