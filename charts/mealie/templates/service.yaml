apiVersion: v1
kind: Service
metadata:
  name: {{ template "common.fullname" . }}
  labels: {{ include "mealie.objectLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      targetPort: 9000
      port: 9000
      protocol: TCP
  selector: {{ include "mealie.selectorLabels" . | nindent 4 }}
